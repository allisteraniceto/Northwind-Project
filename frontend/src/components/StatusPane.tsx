import "../styles/StatusPane.css";

interface Props {
  employeePicture: string;
}

export default function StatusPane({ employeePicture }: Props) {
  return (
    <>
      <nav className="has-form-completed">
        icon
        <img src={"../assets/"} />
      </nav>
    </>
  );
}
